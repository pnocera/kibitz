{
  "type": "object",
  "additionalProperties": false,
  "required": [
    "advisories"
  ],
  "properties": {
    "advisories": {
      "type": "array",
      "description": "Whatever is worth saying. An empty array is a valid and often correct answer.",
      "items": {
        "type": "object",
        "additionalProperties": false,
        "required": [
          "kind",
          "note",
          "why_it_matters",
          "evidence",
          "confidence"
        ],
        "properties": {
          "kind": {
            "type": "string",
            "description": "A word or two, in your own words, for what sort of remark this is. There is no fixed vocabulary and no severity scale \u2014 describe it however fits."
          },
          "note": {
            "type": "string",
            "description": "The remark itself, in one or two sentences."
          },
          "why_it_matters": {
            "type": "string",
            "description": "Why this is worth the reader's attention right now."
          },
          "evidence": {
            "type": "string",
            "description": "A concrete anchor if one applies: file:line, a command, an observed output. Empty string when the remark is a suggestion rather than a claim about existing code."
          },
          "confidence": {
            "type": "number",
            "minimum": 0,
            "maximum": 1,
            "description": "Your own confidence that this remark is correct and worth making."
          }
        }
      }
    }
  }
}
